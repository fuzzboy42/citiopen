# Copy to .env for local development:
#   cp .env.example .env

DEBUG=True
SECRET_KEY=local-dev-only-change-me

# SQLite by default (no Postgres install required).
# DATABASE_URL=sqlite:///db.sqlite3

# Optional: use local Postgres instead
# DATABASE_URL=postgres://iosue:password@localhost:5432/citiopen
